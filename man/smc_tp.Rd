% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smc_tp.R
\name{smc_tp}
\alias{smc_tp}
\title{Run meta analsyis and compute standardised mean change (SMC) estimates from time points}
\usage{
smc_tp(
  data,
  mean_from = "mean_baseline",
  sd_from = "sd_baseline",
  outcome_mean,
  outcome_sd,
  sample_size = "n_in_arm",
  study_id = "author_arm",
  time_unit,
  time_point,
  hedges = FALSE,
  plot = FALSE,
  save_path = NULL,
  heterogeneity = FALSE
)
}
\arguments{
\item{data}{A data frame containing the study data.}

\item{mean_from}{Name of the column with baseline mean values (default is `"mean_baseline"`).}

\item{sd_from}{Name of the column with baseline standard deviation values (default is `"sd_baseline"`).}

\item{outcome_mean}{Name of the column with mean values at the specified outcome time point.}

\item{outcome_sd}{Name of the column with standard deviation values at the specified outcome time point.}

\item{sample_size}{Name of the column with the sample size for each study (default is `"n_in_arm"`).}

\item{study_id}{Name of the column identifying individual studies (default is `"author_arm"`).}

\item{time_unit}{A character string representing the unit of time (e.g., `"weeks"`).}

\item{time_point}{A numeric value specifying the time point being analyzed.}

\item{hedges}{Logical. If `TRUE`, applies Hedges' g correction for small sample sizes (default is `FALSE`).}

\item{plot}{Logical. If `TRUE`, generates forest and funnel plots for the meta-analysis results (default is `FALSE`).}

\item{save_path}{A character string specifying the file path to save the plots. If `NULL`, plots are not saved (default is `NULL`).}

\item{heterogeneity}{Logical. If `TRUE`, includes heterogeneity statistics (`I²`, Cochran’s Q, and τ²) in the output (default is `FALSE`).}
}
\value{
A data frame containing the following columns:
\itemize{
  \item \code{time_point}: The specified time point.
  \item \code{total_sample}: The total sample size used in the time point meta-analysis.
  \item \code{smc}: The pooled standardised mean change estimate.
  \item \code{lower_ci}: The lower bound of the confidence interval for the pooled SMC.
  \item \code{upper_ci}: The upper bound of the confidence interval for the pooled SMC.
  \item \code{I2} (if \code{heterogeneity = TRUE}): The I² statistic indicating heterogeneity.
  \item \code{Q} (if \code{heterogeneity = TRUE}): Cochran’s Q statistic.
  \item \code{tau2} (if \code{heterogeneity = TRUE}): Between-study variance.
}
}
\description{
This function calculates the standardised mean change (SMC) from a starting time point and an outcome time point.
It optionally applies Hedges' g correction for small sample sizes, performs a random-effects meta-analysis,
and generates forest and funnel plots.
}
\examples{
# Example data frame
data <- data.frame(
  mean_baseline = c(2.1, 2.4, 1.8),
  sd_baseline = c(0.5, 0.6, 0.4),
  mean_2weeks = c(3.1, 3.3, 2.5),
  sd_2weeks = c(0.5, 0.6, 0.5),
  n_in_arm = c(50, 60, 45),
  author_arm = c("Study 1", "Study 2", "Study 3")
)

# Compute SMC without Hedges' correction
smc_tp(data,
       mean_from = "mean_baseline",
       sd_from = "sd_baseline",
       outcome_mean = "mean_2weeks",
       outcome_sd = "sd_2weeks",
       sample_size = "n_in_arm",
       study_id = "author_arm",
       time_unit = "weeks",
       time_point = 2,
       hedges = FALSE,
       plot = TRUE)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pooled_smc_tp.R
\name{pooled_smc_tp}
\alias{pooled_smc_tp}
\title{Run meta-analysis and generate pooled standardised mean change estimates}
\usage{
pooled_smc_tp(
  data,
  outcome_means,
  outcome_sds,
  baseline_mean,
  baseline_sd,
  sample_size,
  time_points,
  time_unit,
  intervention = NULL,
  plot = TRUE
)
}
\arguments{
\item{data}{A data frame containing study-level data.}

\item{outcome_means}{A character vector of column names for outcome means at different time points.}

\item{outcome_sds}{A character vector of column names for outcome standard deviations at different time points.}

\item{baseline_mean}{The column name for the baseline mean.}

\item{baseline_sd}{The column name for the baseline standard deviation.}

\item{sample_size}{The column name for the sample size.}

\item{time_points}{A character vector of time points corresponding to `outcome_means` and `outcome_sds`.}

\item{time_unit}{A string specifying the time unit (e.g., "weeks").}

\item{intervention}{A string specifying the intervention name (optional).}

\item{plot}{Logical, whether to plot the pooled results.}
}
\value{
A data frame containing pooled SMC estimates and confidence intervals for each time point.
}
\description{
Run meta-analysis and generate pooled standardised mean change estimates
}
\examples{
data <- data.frame(
  study = c("Study 1", "Study 2", "Study 3"),
  mean_baseline = c(10, 12, 11),
  sd_baseline = c(2, 3, 2.5),
  mean_1week = c(8, 10, 9),
  sd_1week = c(1.5, 2.5, 2),
  mean_2weeks = c(7, 9, 8),
  sd_2weeks = c(1.2, 2.3, 1.8),
  n_in_arm = c(50, 60, 55)
)
outcome_means <- c("mean_1week", "mean_2weeks")
outcome_sds <- c("sd_1week", "sd_2weeks")
time_points <- c("1", "2")
time_unit <- "weeks"
results <- pooled_smc_tp(data, outcome_means, outcome_sds, "mean_baseline",
                         "sd_baseline", "n_in_arm", time_points, time_unit)
}
